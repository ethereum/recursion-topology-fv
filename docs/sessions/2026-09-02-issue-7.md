# Session 2026-09-02 — full Vanilla VM assembly (Issue 7)

## Bootstrap
- **Issue:** `PLAN.md` → Issue 7 (assemble the full Vanilla VM and state the main theorem).
- **Branch:** `yl-issue-7`, created from `main` at `85ef683` (merged Issue 5).
- **Read at start:** `README.md`, `INVARIANTS.md`, `CONVENTIONS.md`, `PLAN.md`,
  `CORRESPONDENCE.md`, `STEP_INTERFACES.md`, `PAPER_REVISION.md`, the math
  companion, and the Issue 1/3/4/5 Lean modules and sanity models.
- **Build at start:** `lake build` green at `85ef683` (8602 jobs).

## What changed
- `VMs/VanillaVM/VanillaVM.lean` uses the bus-checked segment verifier for the
  base proofs of `MultiStep.System`. It derives the required segment extractor
  from `Bus.System.segment_extract` and proves `cte_main` by applying the
  existing recursion and memory-reconstruction theorem.
- `VMs/VanillaVM/VanillaVMSanity.lean` gives a private two-step model in which
  every assumption holds and the final verifier accepts a proof whose two
  segments each contain the required Keccak bus entry; the final VM's plain
  step relation accepts the corresponding transition as well.
- The TwoStep, bus-checked TwoStep, and MultiStep assumption structures now
  include the memory-commitment properties consumed by their CTE theorems
  (GitHub #30).
- The math companion moved from the Lean source tree to `docs/` and live links
  were updated (GitHub #24).
- The misleading “one line per issue” CI comment was corrected (GitHub #29),
  and `cte_main` was added to the headline axiom audit.
- New public surface (I5): `VanillaVM.System`,
  `VanillaVM.System.toMultiStep`, `VanillaVM.System.toZkVM`,
  `VanillaVM.System.Assumptions`, and `VanillaVM.System.cte_main`. The first
  four provide the minimum configuration, the corresponding recursive system,
  the concrete `ZkVM`, and the visible assumptions needed to state the final
  theorem; no new security predicate or step relation is introduced.

## Axiom / `sorry` ledger diff
- `#print axioms VanillaZkVM.VanillaVM.System.cte_main`: `[propext,
  Classical.choice, Quot.sound]`, all permitted.
- `sorry`/`sorryAx`/`admit`/new `axiom` added: none.

## CORRESPONDENCE rows touched
- Added the assembled boundary-committing `ZkVM` and the probability-free
  `cte_main` rows as proved and awaiting human review.
- The main-theorem row remains explicitly incomplete until Issue 6 adds the
  advantage bound.

## Adversarial review
- Dimensions: vacuity, theorem composition, public-surface reuse, CI integrity,
  and documentation overclaim.
- The concrete sanity model witnesses the whole assumption bundle together
  with an accepted proof; no contradictory-assumption finding was reproduced.
- The assembled theorem does not separately assume that a base segment proof
  can be extracted: that fact follows from the bus theorem before
  `MultiStep.cte` is invoked.
- GitHub Issue 7's older body still mentions named breaks and a quantitative
  dependency; the current `PLAN.md` expressly supersedes it with a
  probability-free theorem followed later by Issues 10 and 6.

## Build at end
- `lake build`: green (8604 jobs).
- Full CI audit: green; 72 correspondence declarations elaborate, all 24
  project modules pass hygiene, and every headline theorem uses only the
  permitted axioms.

## Handoff note
- Dmitry and Benedikt should review the `VanillaVM.System`/`Assumptions`
  statements and `cte_main`, especially the boundary commitment and the fact
  that `segment_extract`—rather than a separate assumption—supplies each base
  segment to the recursive proof.
- GitHub #22 remains deliberately untouched because choosing a repository
  license requires owner confirmation. Broad documentation rewriting in #25
  remains separate as requested.
